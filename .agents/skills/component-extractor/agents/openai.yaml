interface:
  display_name: "Component Extractor"
  short_description: "Extract naturally described slide scopes into a reviewed visual library"
  default_prompt: "Use $component-extractor to interpret my natural-language slide scope, extract the selected regions or full slides, stage them with evidence, and publish only after approval."
