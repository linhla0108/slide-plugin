interface:
  display_name: "Component Extractor"
  short_description: "Extract selected slide regions into a reviewed visual library"
  default_prompt: "Use $component-extractor to extract the exact slide regions I identify, stage them with evidence, and publish only after approval."
