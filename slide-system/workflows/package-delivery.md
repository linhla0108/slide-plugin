# Package Delivery

1. Validate the run manifest.
2. Verify requested exports and QA reports exist.
3. Compute checksums for final artifacts.
4. Copy or reference only run-owned files.
5. Write a concise delivery report with paths, tests, limitations, and overrides.
6. Run `scripts/package_job.py` to create the delivery manifest.
