# Shared Visual Library

Published extraction artifacts are stored by type in this directory. Canonical
brand assets remain in their brand skill and are referenced from the registry.

Do not manually make staging items available to generation. Use
`scripts/publish_extraction.py` after per-item approval.

