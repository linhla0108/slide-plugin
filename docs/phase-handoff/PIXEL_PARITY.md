# Legacy Pixel-Parity Guide

The current render-equivalence contract is:

- `../../slide-system/workflows/verify-render-parity.md`
- `../../slide-system/scripts/compare_renders.py`

The workflow requires equal-size HTML and export renders, side-by-side proof,
overlay, amplified diff, metrics, and documented accepted differences.

