# Legacy Phase Checklist

Use the new workflow set under `../../slide-system/workflows/`.

For generation, follow requirement check, planning, visual selection, approval,
build, export, render verification, and package delivery. For extraction,
follow manual request validation, deduplication, staging QA, per-item approval,
publication, and catalog rebuild.

