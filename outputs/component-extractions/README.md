# Component Extractions

This directory contains manual extraction staging packages. Generate packages
with `slide-system/scripts/scaffold_extraction.py`. Only explicitly approved
items may be published to the shared library.

